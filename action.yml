name: Dewatermark Scan
description: Detect suspicious Unicode artifacts and emit GitHub-compatible SARIF.
author: Cyrus Anfar
branding:
  icon: search
  color: purple
inputs:
  paths:
    description: Space-separated paths to scan
    default: .
  output:
    description: SARIF output path
    default: dewatermark.sarif
runs:
  using: composite
  steps:
    - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
      with:
        python-version: "3.12"
    - name: Install dewatermark action
      shell: bash
      run: python -m pip install "$GITHUB_ACTION_PATH"
    - name: Scan repository
      shell: bash
      env:
        INPUT_PATHS: ${{ inputs.paths }}
        INPUT_OUTPUT: ${{ inputs.output }}
      run: dewatermark check $INPUT_PATHS --format sarif --output "$INPUT_OUTPUT" || test $? -eq 1
