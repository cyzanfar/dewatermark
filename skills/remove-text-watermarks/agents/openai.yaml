interface:
  display_name: "Remove Text Watermarks"
  short_description: "Clean hidden Unicode and verify known watermark mitigation"
  default_prompt: "Use $remove-text-watermarks to inspect text, locate known detector signals when possible, request explicit consent, apply the least destructive reviewed cleanup, and verify only the scoped result."
