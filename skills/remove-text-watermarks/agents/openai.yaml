interface:
  display_name: "Remove Text Watermarks"
  short_description: "Inspect and clean hidden Unicode text artifacts safely"
  default_prompt: "Use $remove-text-watermarks to inspect text, create a content-bound plan, request explicit consent, apply the least destructive reviewed cleanup, and verify the scoped result."
